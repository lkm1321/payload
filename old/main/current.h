void currentSetup(void);     // Returns results from INA219 current sensor
float* currentAcquire(void);         // Initialises the current sensor

float *currentData;    // Pointer to returned array of current sensor results


